from setuptools import setup, find_packages

setup(
    name='zerru',
    version='0.0.2',
    author='ZerProg',install_requires=['tqdm'],
    description='Zer-ru — это модуль zer на русском! Для сокращения и автоматизации кода.',
    packages=find_packages()
)