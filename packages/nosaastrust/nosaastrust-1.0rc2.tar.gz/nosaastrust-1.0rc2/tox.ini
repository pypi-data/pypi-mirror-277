[tox]
requires =
    tox >= 4.0
env_list =
    lint
    py312, py311

[testenv]
deps = cryptography
extras = test
set_env = \
    NOSTDIR = tests/testenv
    BORG_PASSPHRASE = test
commands = pytest -rav --ignore=tests/testenv

[testenv:lint]
extras = lint
commands = flake8
