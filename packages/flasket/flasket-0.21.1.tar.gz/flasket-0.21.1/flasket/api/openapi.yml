---
#
# https://github.com/OAI/OpenAPI-Specification/blob/master/versions/3.1.0.md
#
openapi: "3.0.0"

# set some required defaults
info:
  version: "0.0.0"
  title: "Flasket OpenAPI Server"
  description: None

servers:
  - url: /

paths:
  x-dummy: # prevents the key from being deleted

components:
  parameters: # nothing, cf x-parameters.yml
  responses:  # nothing, cf x-responses.yml
  schemas:    # nothing, cf x-schemas.yml
