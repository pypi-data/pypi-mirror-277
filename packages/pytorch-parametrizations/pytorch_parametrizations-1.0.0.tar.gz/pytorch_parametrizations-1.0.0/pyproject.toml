[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pytorch-parametrizations"
version = "1.0.0"
description = "Parametrizations for PyTorch"
readme = "README.md"
authors = [{name = "Michał Dyczko", email = "michal@dyczko.dev"}]
license = { file = "LICENSE" }
keywords = ["pytorch", "parametrizations", "sadam", "spectral Adam"]
dependencies = [
    "torch>=2.2.1",
]
requires-python = ">=3.8"

[project.urls]
Homepage = "https://github.com/michaldyczko/pytorch-parametrizations"