[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "pybind11>=2.6.1"
]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 120
target-version = ['py38']
include = '\.pyi?$'
extend-exclude = '''
'''